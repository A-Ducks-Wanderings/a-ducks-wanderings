{
	id: "charles",
	editor_info: {
		category: "player"
	},
	properties: {
		speed_x: { type: "decimal", default: 0.0, persistent: false },
		speed_y: { type: "decimal", default: 0.0, persistent: false },
		ctrl_run: { type: "bool", default: false, persistent: false },
		friction: { type: "decimal", default: 0.5, persistent: false },
		_key_state: { type: "{string -> bool}", default: {} },
	},

	on_end_stand_anim: "[
		set(animation, 'stand')
	]",

	on_end_walk_anim: "[
		set(animation, 'walk')
	]",

	on_process: "[
		map(ctrl_keys, 'key', [
			if((key in ctrl_keys), [
				if(_key_state[key] = false, [
					set(_key_state[key], true),
					fire_event('on_ctrl_' + key),
					debug('on_ctrl_' + key)
				], [
					fire_event('on_held_key_' + key),
					debug('on_held_key_' + key)
				])
			], [
				set(_key_state[key], false),
				fire_event('on_release_key_' + key),
				debug('on_release_key_' + key)
			])
		]),
		set(ctrl_run, ('d' in ctrl_keys)),
		if(is_standing, [
			set(animation, 'stand')
		]),
		if((ctrl_left or ctrl_right), [
			set(animation, 'walk')
		]),
		if(animation in ['walk', 'stand'] and not is_standing, [
			add(speed_y, 2)
		]),
		if(y > level.dimensions[3], [
			die()
		]),
		set(speed_x, speed_x * friction),
		add(x, speed_x),
		add(y, speed_y)
	]",

	on_process_walk: "[
		if(ctrl_left, [
			add(speed_x, -5)
		]),
		if(ctrl_right, [
			add(speed_x, 5)
		])
	]",

	animation: [
		{
			"@base": true,
			image: "characters/charles-spritesheet1.png",
			pad: 3,
			solid_area: [10, 4, 21, 30],
		},
		{
			id: "stand",
			rect: [2, 2, 33, 33],
			duration: 12,
			frames: 2,
		},
		{
			id: "walk",
			rect: [2, 72, 33, 103],
			duration: 4,
			frames: 4,
		},
	]
}